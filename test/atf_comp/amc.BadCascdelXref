# start amc cmd:"$bindir/amc -in_dir:test/amc/bad_cascdel_xref.ssim -out_dir: -report:N zzz"
# eof amc
amc -> amc.cascdel_xref  field:algo_lib.FDb.c_row  reftype:Ptrary  comment:"cascdel deletes rows until the index is empty; without dmmeta.xref the row's delete cannot unlink it"
amc -> amc.no_output  comment:"no files were modified"
# exit amc code:1
