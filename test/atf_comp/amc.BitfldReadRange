# start amc cmd:"$bindir/amc -in_dir:test/amc/bitfld_read_range.ssim -out_dir: -report:N 'algo_lib.Rec.(narrow|full|signed_full|flag|qos|sig|sig_edge|sig_full|dec|plain).ReadStrptrMaybe'"
# eof amc
amc -> // --- algo_lib.Rec.narrow.ReadStrptrMaybe
amc -> // Convert string to field. Return success value
amc -> bool algo_lib::narrow_ReadStrptrMaybe(algo_lib::Rec& parent, algo::strptr rhs) {
amc ->     bool retval = false;
amc ->     retval = narrow_SetStrptrMaybe(parent,rhs); // try symbol conversion
amc ->     if (!retval) { // didn't work? try reading as underlying type
amc ->         u32 narrow_tmp;
amc ->         retval = u32_ReadStrptrMaybe(narrow_tmp, rhs);
amc ->         retval = retval && (narrow_tmp <= 0x07);// value must fit the field's store
amc ->         if (retval) {
amc ->             narrow_Set(parent, narrow_tmp);
amc ->         }
amc ->     }
amc ->     return retval;
amc -> }
amc -> // --- algo_lib.Rec.full.ReadStrptrMaybe
amc -> // Convert string to field. Return success value
amc -> bool algo_lib::full_ReadStrptrMaybe(algo_lib::Rec& parent, algo::strptr rhs) {
amc ->     bool retval = false;
amc ->     retval = full_SetStrptrMaybe(parent,rhs); // try symbol conversion
amc ->     if (!retval) { // didn't work? try reading as underlying type
amc ->         u8 full_tmp;
amc ->         retval = u8_ReadStrptrMaybe(full_tmp, rhs);
amc ->         if (retval) {
amc ->             full_Set(parent, full_tmp);
amc ->         }
amc ->     }
amc ->     return retval;
amc -> }
amc -> // --- algo_lib.Rec.signed_full.ReadStrptrMaybe
amc -> // Convert string to field. Return success value
amc -> bool algo_lib::signed_full_ReadStrptrMaybe(algo_lib::Rec& parent, algo::strptr rhs) {
amc ->     bool retval = false;
amc ->     retval = signed_full_SetStrptrMaybe(parent,rhs); // try symbol conversion
amc ->     if (!retval) { // didn't work? try reading as underlying type
amc ->         i8 signed_full_tmp;
amc ->         retval = i8_ReadStrptrMaybe(signed_full_tmp, rhs);
amc ->         if (retval) {
amc ->             signed_full_Set(parent, signed_full_tmp);
amc ->         }
amc ->     }
amc ->     return retval;
amc -> }
amc -> // --- algo_lib.Rec.flag.ReadStrptrMaybe
amc -> // Convert string to field. Return success value
amc -> bool algo_lib::flag_ReadStrptrMaybe(algo_lib::Rec& parent, algo::strptr rhs) {
amc ->     bool retval = false;
amc ->     retval = flag_SetStrptrMaybe(parent,rhs); // try symbol conversion
amc ->     if (!retval) { // didn't work? try reading as underlying type
amc ->         bool flag_tmp;
amc ->         retval = bool_ReadStrptrMaybe(flag_tmp, rhs);
amc ->         if (retval) {
amc ->             flag_Set(parent, flag_tmp);
amc ->         }
amc ->     }
amc ->     return retval;
amc -> }
amc -> // --- algo_lib.Rec.qos.ReadStrptrMaybe
amc -> // Convert string to field. Return success value
amc -> bool algo_lib::qos_ReadStrptrMaybe(algo_lib::Rec& parent, algo::strptr rhs) {
amc ->     bool retval = false;
amc ->     retval = qos_SetStrptrMaybe(parent,rhs); // try symbol conversion
amc ->     if (!retval) { // didn't work? try reading as underlying type
amc ->         algo_lib::Qos qos_tmp;
amc ->         retval = algo_lib::Qos_ReadStrptrMaybe(qos_tmp, rhs);
amc ->         retval = retval && (qos_tmp.value <= 0x0f);// value must fit the field's store
amc ->         if (retval) {
amc ->             qos_Set(parent, qos_tmp);
amc ->         }
amc ->     }
amc ->     return retval;
amc -> }
amc -> // --- algo_lib.Rec.sig.ReadStrptrMaybe
amc -> // Convert string to field. Return success value
amc -> bool algo_lib::sig_ReadStrptrMaybe(algo_lib::Rec& parent, algo::strptr rhs) {
amc ->     bool retval = false;
amc ->     retval = sig_SetStrptrMaybe(parent,rhs); // try symbol conversion
amc ->     if (!retval) { // didn't work? try reading as underlying type
amc ->         algo_lib::Sig sig_tmp;
amc ->         retval = algo_lib::Sig_ReadStrptrMaybe(sig_tmp, rhs);
amc ->         retval = retval && (sig_tmp.value >= 0 && sig_tmp.value <= 0x0f);// value must fit the field's store
amc ->         if (retval) {
amc ->             sig_Set(parent, sig_tmp);
amc ->         }
amc ->     }
amc ->     return retval;
amc -> }
amc -> // --- algo_lib.Rec.dec.ReadStrptrMaybe
amc -> bool algo_lib::dec_ReadStrptrMaybe(algo_lib::Rec& parent, algo::strptr in) {
amc ->     int index = 0;
amc ->     int neg = false;
amc ->     // skip leading sign
amc ->     if (index < in.n_elems && (in.elems[index] == '+' || in.elems[index] == '-')) {
amc ->         neg = in.elems[index]=='-';
amc ->         index++;
amc ->     };
amc ->     // parse digits
amc ->     u64 val = 0;
amc ->     int ndot = 0; // number of . encountered
amc ->     int nfrac = 0; // read up to NFRAC digits after .; if not found, fill in
amc ->     bool ok = true;// overflow or bad char
amc ->     u64 limdiv = 409ULL;// largest magnitude the field holds for this sign, over 10
amc ->     u64 limmod = 5ULL;// and that magnitude's last digit
amc ->     for (; index < in.n_elems; index++) {
amc ->         char c = in.elems[index];
amc ->         if (c == '.') {
amc ->             ok &= ndot == 0; // max 1 dot
amc ->             ndot++;
amc ->         } else if (algo_lib::DigitCharQ(c)) {
amc ->             if (nfrac < 2) { // ignore digits after 2'th
amc ->                 u64 digit = c - '0';
amc ->                 ok &= val < limdiv || (val == limdiv && digit <= limmod);// val*10+digit must fit the field
amc ->                 val = val*10 + digit;
amc ->                 nfrac += ndot>0;
amc ->             }
amc ->         } else {
amc ->             ok = false; // unknown char
amc ->         }
amc ->     }
amc ->     while (nfrac < 2) {// insert missing
amc ->         ok &= val <= limdiv;// val*10 must fit the field
amc ->         val = val*10;
amc ->         nfrac++;
amc ->     }
amc ->     u64 final_val = val;
amc ->     ok &= !neg;
amc ->     if (ok) {
amc ->         dec_Set(parent, u32(final_val));// store value
amc ->     }
amc ->     return ok;
amc -> }
amc -> // --- algo_lib.Rec.sig_edge.ReadStrptrMaybe
amc -> // Convert string to field. Return success value
amc -> bool algo_lib::sig_edge_ReadStrptrMaybe(algo_lib::Rec& parent, algo::strptr rhs) {
amc ->     bool retval = false;
amc ->     retval = sig_edge_SetStrptrMaybe(parent,rhs); // try symbol conversion
amc ->     if (!retval) { // didn't work? try reading as underlying type
amc ->         algo_lib::Sig8 sig_edge_tmp;
amc ->         retval = algo_lib::Sig8_ReadStrptrMaybe(sig_edge_tmp, rhs);
amc ->         retval = retval && (sig_edge_tmp.value >= 0);// value must fit the field's store
amc ->         if (retval) {
amc ->             sig_edge_Set(parent, sig_edge_tmp);
amc ->         }
amc ->     }
amc ->     return retval;
amc -> }
amc -> // --- algo_lib.Rec.sig_full.ReadStrptrMaybe
amc -> // Convert string to field. Return success value
amc -> bool algo_lib::sig_full_ReadStrptrMaybe(algo_lib::Rec& parent, algo::strptr rhs) {
amc ->     bool retval = false;
amc ->     retval = sig_full_SetStrptrMaybe(parent,rhs); // try symbol conversion
amc ->     if (!retval) { // didn't work? try reading as underlying type
amc ->         algo_lib::Sig8 sig_full_tmp;
amc ->         retval = algo_lib::Sig8_ReadStrptrMaybe(sig_full_tmp, rhs);
amc ->         if (retval) {
amc ->             sig_full_Set(parent, sig_full_tmp);
amc ->         }
amc ->     }
amc ->     return retval;
amc -> }
amc -> // --- algo_lib.Rec.plain.ReadStrptrMaybe
amc -> inline static bool algo_lib::plain_ReadStrptrMaybe(algo_lib::Rec& parent, algo::strptr in_str) {
amc ->     bool retval = true;
amc ->     u32 plain_tmp;
amc ->     retval = u32_ReadStrptrMaybe(plain_tmp, in_str);
amc ->     if (retval) {
amc ->         plain_Set(parent, plain_tmp);
amc ->     }
amc ->     return retval;
amc -> }
# exit amc code:0
