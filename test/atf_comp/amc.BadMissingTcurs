# start amc cmd:"$bindir/amc -in_dir:test/amc/missing_tcurs.ssim -out_dir: -report:N zzz"
# eof amc
amc -> amc.bad_fcurs  fcurs:algo_lib.FDb.c_row/curs  reftype:Ptrary  comment:"Cursor exists, but its amcdb.tcurs row is absent from the input set; add the row"
amc -> amc.missing_tcurs  tfunc:Ptrary.curs  field:algo_lib.FDb.c_row  comment:"cursor generator without its amcdb.tcurs row; add the row to the input set"
amc -> amc.no_output  comment:"no files were modified"
# exit amc code:2
