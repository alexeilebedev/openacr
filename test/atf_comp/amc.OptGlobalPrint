# start amc cmd:"$bindir/amc -in_dir:test/amc/opt_global.ssim -out_dir: -report:N algo_lib.FDb.tail.Print"
# eof amc
amc -> amc.infinity_pool  field:algo_lib.FDb._db  arg:algo_lib.FDb  reftype:Global  comment:"Varlen pool must be used for this ctype"
amc -> // --- algo_lib.FDb.tail.Print
amc -> void algo_lib::tail_Print(cstring &out) {
amc ->     if (algo_lib::Sub *tail = tail_Get()) {
amc ->         out << *tail;
amc ->     }
amc -> }
amc -> amc.no_output  comment:"no files were modified"
# exit amc code:1
