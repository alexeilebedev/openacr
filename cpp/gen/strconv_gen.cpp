//
// cpp/gen/strconv_gen.cpp
// Generated by AMC
//
// (C) AlgoEngineering LLC 2008-2013
// (C) NYSE | Intercontinental Exchange 2013-2016
//


#include "include/algo.h"  // hard-coded include
#include "include/gen/strconv_gen.h"
#include "include/gen/strconv_gen.inl.h"
#include "include/gen/command_gen.h"
#include "include/gen/command_gen.inl.h"
#include "include/gen/lib_json_gen.h"
#include "include/gen/lib_json_gen.inl.h"
#include "include/gen/lib_prot_gen.h"
#include "include/gen/lib_prot_gen.inl.h"
#include "include/gen/algo_lib_gen.h"
#include "include/gen/algo_lib_gen.inl.h"
//#pragma endinclude

// Instantiate all libraries linked into this executable,
// in dependency order
lib_json::FDb   lib_json::_db;    // dependency found via dev.targdep
algo_lib::FDb   algo_lib::_db;    // dependency found via dev.targdep
strconv::FDb    strconv::_db;     // dependency found via dev.targdep

namespace strconv {
const char *strconv_help =
"strconv: A simple string utility\n"
"Usage: strconv str [options]\n"
"    [str]          string  String parameter\n"
"    -tocamelcase           Convert string to camel case. default: false\n"
"    -tolowerunder          Convert string to lower-under. default: false\n"
"    -in            string  Input directory or filename, - for stdin. default: \"data\"\n"
"    -pathcomp      string  Extract path component from string\n"
"    -verbose               Enable verbose mode\n"
"    -debug                 Enable debug mode\n"
"    -version               Show version information\n"
"    -sig                   Print SHA1 signatures for dispatches\n"
"    -help                  Print this screen and exit\n"
;


const char *strconv_syntax =
"[str]:string\n"
" -tocamelcase:flag\n"
" -tolowerunder:flag\n"
" -in:string=\"data\"\n"
" -pathcomp:string=\n"
;
} // namespace strconv
namespace strconv {
    // Load statically available data into tables, register tables and database.
    static void          InitReflection();
    // find trace by row id (used to implement reflection)
    static algo::ImrowPtr trace_RowidFind(int t) __attribute__((nothrow));
    // Function return 1
    static i32           trace_N() __attribute__((__warn_unused_result__, nothrow, pure));
    static void          SizeCheck();
} // end namespace strconv

// --- strconv.trace..Print
// print string representation of strconv::trace to string LHS, no header -- cprint:strconv.trace.String
void strconv::trace_Print(strconv::trace & row, algo::cstring &str) {
    algo::tempstr temp;
    str << "strconv.trace";
    (void)row;//only to avoid -Wunused-parameter
}

// --- strconv.FDb._db.MainArgs
// Main function
void strconv::MainArgs(int argc, char **argv) {
    Argtuple argtuple;
    Argtuple_ReadArgv(argtuple, argc,argv,strconv_syntax, strconv_help);
    vrfy(strconv_ReadTupleMaybe(strconv::_db.cmdline, argtuple.tuple),"where:read_cmdline");
    vrfy(strconv::LoadTuplesMaybe(strconv::_db.cmdline.in)
    ,tempstr()<<"where:load_input  "<<algo_lib::DetachBadTags());
    strconv::Main(); // call through to user-defined main
}

// --- strconv.FDb._db.MainLoop
// Main loop.
void strconv::MainLoop() {
    algo::SchedTime time(algo::get_cycles());
    algo_lib::_db.clock          = time;
    do {
        algo_lib::_db.next_loop.value = algo_lib::_db.limit;
        algo_lib::Step(); // dependent namespace specified via (dev.targdep)
    } while (algo_lib::_db.next_loop < algo_lib::_db.limit);
}

// --- strconv.FDb._db.Step
// Main step
void strconv::Step() {
}

// --- strconv.FDb._db.InitReflection
// Load statically available data into tables, register tables and database.
static void strconv::InitReflection() {
    algo_lib::imdb_InsertMaybe(algo::Imdb("strconv", NULL, NULL, strconv::MainLoop, NULL, algo::Comment()));

    algo::Imtable t_trace;
    t_trace.imtable         = "strconv.trace";
    t_trace.ssimfile        = "";
    t_trace.size            = sizeof(strconv::trace);
    t_trace.comment.value   = "";
    t_trace.c_RowidFind     = trace_RowidFind;
    t_trace.NItems          = trace_N;
    t_trace.Print           = (algo::ImrowPrintFcn)strconv::trace_Print;
    algo_lib::imtable_InsertMaybe(t_trace);


    // -- load signatures of existing dispatches --
}

// --- strconv.FDb._db.StaticCheck
void strconv::StaticCheck() {
    algo_assert(_offset_of(strconv::FieldId, value) + sizeof(((strconv::FieldId*)0)->value) == sizeof(strconv::FieldId));
}

// --- strconv.FDb._db.InsertStrptrMaybe
// Parse strptr into known type and add to database.
// Return value is true unless an error occurs. If return value is false, algo_lib::_db.errtext has error text
bool strconv::InsertStrptrMaybe(algo::strptr str) {
    bool retval = true;
    (void)str;//only to avoid -Wunused-parameter
    return retval;
}

// --- strconv.FDb._db.LoadTuplesMaybe
// Load all finputs from given directory.
bool strconv::LoadTuplesMaybe(algo::strptr root) {
    bool retval = true;
    (void)root;//only to avoid -Wunused-parameter
    return retval;
}

// --- strconv.FDb._db.LoadSsimfileMaybe
// Load specified ssimfile.
bool strconv::LoadSsimfileMaybe(algo::strptr fname) {
    bool retval = true;
    if (FileQ(fname)) {
        retval = algo_lib::LoadTuplesFile(fname, strconv::InsertStrptrMaybe, true);
    }
    return retval;
}

// --- strconv.FDb._db.XrefMaybe
// Insert row into all appropriate indices. If error occurs, store error
// in algo_lib::_db.errtext and return false. Caller must Delete or Unref such row.
bool strconv::_db_XrefMaybe() {
    bool retval = true;
    return retval;
}

// --- strconv.FDb.trace.RowidFind
// find trace by row id (used to implement reflection)
static algo::ImrowPtr strconv::trace_RowidFind(int t) {
    return algo::ImrowPtr(t==0 ? u64(&_db.trace) : u64(0));
}

// --- strconv.FDb.trace.N
// Function return 1
inline static i32 strconv::trace_N() {
    return 1;
}

// --- strconv.FDb..Init
// Set all fields to initial values.
void strconv::FDb_Init() {

    strconv::InitReflection();
}

// --- strconv.FDb..Uninit
void strconv::FDb_Uninit() {
    strconv::FDb &row = _db; (void)row;
}

// --- strconv.FieldId.value.ToCstr
// Convert numeric value of field to one of predefined string constants.
// If string is found, return a static C string. Otherwise, return NULL.
const char* strconv::value_ToCstr(const strconv::FieldId& parent) {
    const char *ret = NULL;
    switch(value_GetEnum(parent)) {
        case strconv_FieldId_value         : ret = "value";  break;
    }
    return ret;
}

// --- strconv.FieldId.value.Print
// Convert value to a string. First, attempt conversion to a known string.
// If no string matches, print value as a numeric value.
void strconv::value_Print(const strconv::FieldId& parent, algo::cstring &lhs) {
    const char *strval = value_ToCstr(parent);
    if (strval) {
        lhs << strval;
    } else {
        lhs << parent.value;
    }
}

// --- strconv.FieldId.value.SetStrptrMaybe
// Convert string to field.
// If the string is invalid, do not modify field and return false.
// In case of success, return true
bool strconv::value_SetStrptrMaybe(strconv::FieldId& parent, algo::strptr rhs) {
    bool ret = false;
    switch (elems_N(rhs)) {
        case 5: {
            switch (u64(algo::ReadLE32(rhs.elems))|(u64(rhs[4])<<32)) {
                case LE_STR5('v','a','l','u','e'): {
                    value_SetEnum(parent,strconv_FieldId_value); ret = true; break;
                }
            }
            break;
        }
    }
    return ret;
}

// --- strconv.FieldId.value.SetStrptr
// Convert string to field.
// If the string is invalid, set numeric value to DFLT
void strconv::value_SetStrptr(strconv::FieldId& parent, algo::strptr rhs, strconv_FieldIdEnum dflt) {
    if (!value_SetStrptrMaybe(parent,rhs)) value_SetEnum(parent,dflt);
}

// --- strconv.FieldId.value.ReadStrptrMaybe
// Convert string to field. Return success value
bool strconv::value_ReadStrptrMaybe(strconv::FieldId& parent, algo::strptr rhs) {
    bool retval = false;
    retval = value_SetStrptrMaybe(parent,rhs); // try symbol conversion
    if (!retval) { // didn't work? try reading as underlying type
        retval = i32_ReadStrptrMaybe(parent.value,rhs);
    }
    return retval;
}

// --- strconv.FieldId..ReadStrptrMaybe
// Read fields of strconv::FieldId from an ascii string.
// The format of the string is the format of the strconv::FieldId's only field
bool strconv::FieldId_ReadStrptrMaybe(strconv::FieldId &parent, algo::strptr in_str) {
    bool retval = true;
    retval = retval && strconv::value_ReadStrptrMaybe(parent, in_str);
    return retval;
}

// --- strconv.FieldId..Print
// print string representation of strconv::FieldId to string LHS, no header -- cprint:strconv.FieldId.String
void strconv::FieldId_Print(strconv::FieldId & row, algo::cstring &str) {
    strconv::value_Print(row, str);
}

// --- strconv...main
int main(int argc, char **argv) {
    try {
        lib_json::FDb_Init();
        algo_lib::FDb_Init();
        strconv::FDb_Init();
        algo_lib::_db.argc = argc;
        algo_lib::_db.argv = argv;
        algo_lib::IohookInit();
        strconv::MainArgs(algo_lib::_db.argc,algo_lib::_db.argv); // dmmeta.main:strconv
    } catch(algo_lib::ErrorX &x) {
        prerr("strconv.error  " << x); // there may be additional hints in DetachBadTags
        algo_lib::_db.exit_code = 1;
    }
    if (algo_lib::_db.last_signal) {
        algo_lib::_db.exit_code = 1;
    }
    try {
        strconv::FDb_Uninit();
        algo_lib::FDb_Uninit();
        lib_json::FDb_Uninit();
    } catch(algo_lib::ErrorX &) {
        // don't print anything, might crash
        algo_lib::_db.exit_code = 1;
    }
    // only the lower 1 byte makes it to the outside world
    (void)i32_UpdateMin(algo_lib::_db.exit_code,255);
    return algo_lib::_db.exit_code;
}

// --- strconv...SizeCheck
inline static void strconv::SizeCheck() {
}
