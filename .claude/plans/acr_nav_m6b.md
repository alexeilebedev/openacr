# M6b: `/` Filter Mode + Mode-Qualified Keybindings

## Context

M6a replaced the string if-chain in `DispatchAction` with gstatic Hook dispatch. Filter handler stubs exist but do nothing. This milestone implements filter mode and mode-qualifies the keybind table so mode-specific key behavior is data, not code.

## Approach

### Phase 1: Schema — mode-qualify keybinds + add filter_backspace

**`data/acr_navdb/navaction.ssim`** — add:
```
acr_navdb.navaction  navaction:filter_backspace  comment:"Delete last filter character"
```

**`data/acr_navdb/keybind.ssim`** — replace all 13 flat records with 15 mode-qualified composite keys (`navmode.keyname`). The existing `ind_keybind` Thash hashes on `acr_navdb.Keybind.keybind` (Smallstr50, unique:Y) — composite keys are just different key strings, no schema changes needed.

```
acr_navdb.keybind  keybind:browse./          navaction:filter_start        comment:""
acr_navdb.keybind  keybind:browse.Backspace  navaction:go_back             comment:""
acr_navdb.keybind  keybind:browse.Down       navaction:move_down           comment:""
acr_navdb.keybind  keybind:browse.Enter      navaction:follow_ref          comment:""
acr_navdb.keybind  keybind:browse.Left       navaction:switch_panel_left   comment:""
acr_navdb.keybind  keybind:browse.PgDown     navaction:page_down           comment:""
acr_navdb.keybind  keybind:browse.PgUp       navaction:page_up             comment:""
acr_navdb.keybind  keybind:browse.Right      navaction:switch_panel_right  comment:""
acr_navdb.keybind  keybind:browse.Up         navaction:move_up             comment:""
acr_navdb.keybind  keybind:browse.j          navaction:move_down           comment:""
acr_navdb.keybind  keybind:browse.k          navaction:move_up             comment:""
acr_navdb.keybind  keybind:browse.q          navaction:quit                comment:""
acr_navdb.keybind  keybind:filter.Backspace  navaction:filter_backspace    comment:""
acr_navdb.keybind  keybind:filter.Enter      navaction:filter_accept       comment:""
acr_navdb.keybind  keybind:filter.Escape     navaction:filter_cancel       comment:""
```

Adding a new mode = adding keybind records, zero code changes.

Run `amc` to regenerate (adds `navaction_filter_backspace` forward declaration).

### Phase 2: Update event loop for composite key lookup

Change keybind lookup from `ind_keybind_Find(key_name)` to:
```cpp
tempstr composite;
composite << _db.p_cur_mode->navmode << "." << key_name;
acr_nav::FKeybind *keybind = ind_keybind_Find(composite);
if (keybind) {
    keybind->p_navaction->step();
}
```

One small fallback for unbound printable chars in filter mode (text input, not action dispatch):
```cpp
bool in_filter = (_db.p_cur_mode->navmode == "filter");
if (!keybind && in_filter && ch_N(key_name) == 1 && ch_qFind(key_name, 0) >= 32) {
    _db.filter << key_name;
    ApplyFilter();
    // reset left panel selection
}
```

### Phase 3: Implement filter logic

**`ApplyFilter()` helper** — rebuilds `zd_sel_ctype` from all ctypes matching both namespace regex AND filter text:
- `zd_sel_ctype_RemoveAll()`
- If filter non-empty: `Regx_ReadSql(regx, "%" + filter + "%", false)` for substring matching
- Re-iterate `_db_ctype_curs`, insert matches

**Refactor `PopulateSelCtype()`** to call `ApplyFilter()` (same code path for initial load and filter updates).

**Fill in handler implementations (replacing M6a stubs):**
- `navaction_filter_start`: set `p_cur_mode` to filter, clear `_db.filter`, focus left panel
- `navaction_filter_cancel`: set `p_cur_mode` to browse, clear `_db.filter`, call `ApplyFilter()`, reset left panel selection
- `navaction_filter_accept`: set `p_cur_mode` to browse (keep filtered list)
- `navaction_filter_backspace`: if `ch_N(_db.filter) > 0`, remove last char, call `ApplyFilter()`, reset left panel selection

**Status bar update in Render():**
- Filter mode: show `/ filter_text_here`
- Browse mode: show keybind help (add `/:filter` to hint text)

### Phase 4: Build and verify

- `acr -check` passes
- `amc` runs clean
- `abt -build acr_nav` compiles
- Manual test matrix:
  1. All browse-mode keybinds work (Up/Down/j/k, Enter, Backspace, Left/Right, PgUp/PgDown, q)
  2. `/` enters filter mode — status bar shows `/:`
  3. Typing narrows the ctype list in real time
  4. Backspace removes last filter char, list expands
  5. Enter accepts filter, returns to browse with filtered list
  6. Escape cancels filter, full list restored
  7. `acr_nav | head` — batch mode still works

## Files modified

| File | Change |
|---|---|
| `data/acr_navdb/navaction.ssim` | Add `filter_backspace` |
| `data/acr_navdb/keybind.ssim` | Replace 13 flat records with 15 mode-qualified |
| `cpp/acr_nav/acr_nav.cpp` | Composite key lookup, filter handlers, ApplyFilter, Render status bar |
| `cpp/gen/`, `include/gen/` | Auto-generated by amc |

## Key design decisions

1. **Mode-qualified composite keys** (`browse.Enter`, `filter.Escape`) — eliminates mode if-chain from event loop. Uses existing Thash — no schema changes to the hash.
2. **`filter_backspace` as navaction** — consistent with filter_start/cancel/accept. All mode-specific key behavior is data-driven.
3. **Printable char fallback** — unbound printable keys in filter mode append to filter text. Text input, not action dispatch — justified asymmetry. Acknowledged as factorization debt.
4. **`Regx_ReadSql` with `%` wrapping** — typing "Db" matches `%Db%`. Matches acr convention.
5. **`ApplyFilter()` unifies initial load and filter** — one code path, no duplication.

## Factorization debt (carried forward, not M6b scope)

- `PanelItemCount` and `Render` dispatch on `panel.position` (0 vs 1) — `ideas.md:194`
- `follow_ref` handler branches on `panel.position` — same issue
- Printable char text input in filter mode is inline code, not data-driven dispatch

## Verification

```bash
acr -check
amc
abt -build acr_nav
acr_nav -ns:amc               # manual TUI test
acr_nav | head                 # batch mode
```
